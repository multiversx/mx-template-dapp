import { ConnectComponent } from './components';

export const NewHome = () => (
  <div className='flex flex-col items-center justify-center bg-accent p-2 max-w-320'>
    <ConnectComponent />
  </div>
);
