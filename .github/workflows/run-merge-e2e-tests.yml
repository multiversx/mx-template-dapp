name: Playwright merge E2E Tests
on:
  pull_request:
    branches: [main, development]
  workflow_dispatch:
jobs:
  # Slack-Notification:
  #   runs-on: runner-dapp
  #   steps:
  #     - name: Slack Notification
  #       uses: rtCamp/action-slack-notify@master
  #       env:
  #         if: always()
  #         SLACK_ICON_EMOJI: ':robot_face:'
  #         SLACK_USERNAME: Playwright Bot
  #         SLACK_MESSAGE: E2E merge tests are now running. A new notification will be sent when completed.
  #         SLACK_TITLE : 'Playwright E2E Template RUN'
  #         SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
  E2E-RUN:
    runs-on: runner-dapp
    # needs: Slack-Notification
    env:
      KEYSTORE1_JSON_B64: ${{ secrets.KEYSTORE1_JSON_B64 }}
      KEYSTORE1_PASSWORD: ${{ secrets.KEYSTORE1_PASSWORD }}
      KEYSTORE1_ADDRESS: ${{ secrets.KEYSTORE1_ADDRESS }}

      KEYSTORE2_JSON_B64: ${{ secrets.KEYSTORE2_JSON_B64 }}
      KEYSTORE2_PASSWORD: ${{ secrets.KEYSTORE2_PASSWORD }}
      KEYSTORE2_ADDRESS: ${{ secrets.KEYSTORE2_ADDRESS }}

      KEYSTORE3_JSON_B64: ${{ secrets.KEYSTORE3_JSON_B64 }}
      KEYSTORE3_PASSWORD: ${{ secrets.KEYSTORE3_PASSWORD }}
      KEYSTORE3_ADDRESS: ${{ secrets.KEYSTORE3_ADDRESS }}

      KEYSTORE4_JSON_B64: ${{ secrets.KEYSTORE4_JSON_B64 }}
      KEYSTORE4_PASSWORD: ${{ secrets.KEYSTORE4_PASSWORD }}
      KEYSTORE4_ADDRESS: ${{ secrets.KEYSTORE4_ADDRESS }}

      KEYSTORE5_PEM_B64: ${{ secrets.KEYSTORE5_PEM_B64 }}
      KEYSTORE5_ADDRESS: ${{ secrets.KEYSTORE5_ADDRESS }}

      KEYSTORE6_PRIVATE_KEY_UTF8: ${{ secrets.KEYSTORE6_PRIVATE_KEY_UTF8 }}
      KEYSTORE6_ADDRESS: ${{ secrets.KEYSTORE6_ADDRESS }}

      METAMASK_MNEMONIC: ${{ secrets.METAMASK_MNEMONIC }}
      METAMASK_ADDRESS: ${{ secrets.METAMASK_ADDRESS }}
      METAMASK_PASSWORD: ${{ secrets.METAMASK_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Setup pnpm
        run: npm install -g pnpm
      - name: Install Dependencies
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 120
          retry_on: error
          max_attempts: 2
          command: pnpm install
      - name: Prepare Environment and Run E2E Tests
        run: |
          # Update and install Chrome/Chromium dependencies
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 libxss1 libasound2 libatk1.0-0 \
            libatk-bridge2.0-0 libcups2 libdrm2 libgbm1 \
            libgtk-3-0 libxcomposite1 libxrandr2 \
            libxdamage1 libxfixes3 libxshmfence1 libxtst6 \
            libpangocairo-1.0-0

          # Install Playwright browsers (with required system deps)
          pnpm exec playwright install --with-deps

          # Setup Synpress (runs MetaMask initialization)
          xvfb-run --auto-servernum --server-args="-screen 0 1280x720x24" \
            pnpm exec synpress tests/test/wallet-setup

          # Run Playwright E2E tests under Xvfb
          xvfb-run --auto-servernum --server-args="-screen 0 1280x720x24" \
            pnpm run run-playwright-test
        shell: bash
        env:
          METAMASK_MNEMONIC: ${{ secrets.METAMASK_MNEMONIC }}
          METAMASK_PASSWORD: ${{ secrets.METAMASK_PASSWORD }}
          METAMASK_ADDRESS: ${{ secrets.METAMASK_ADDRESS }}
      - name: Upload Playwright test report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 1
      # - name: Slack Success Notification
      #   uses: rtCamp/action-slack-notify@master
      #   if: success()
      #   env:
      #     SLACK_ICON_EMOJI: ':robot_face:'
      #     SLACK_USERNAME: Playwright Bot
      #     SLACK_MESSAGE: 'E2E merge Tests completed successfully!'
      #     SLACK_TITLE: 'Playwright E2E Tests - SUCCESS'
      #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
      # - name: Slack Failure Notification
      #   uses: rtCamp/action-slack-notify@master
      #   if: failure()
      #   env:
      #     SLACK_ICON_EMOJI: ':robot_face:'
      #     SLACK_USERNAME: Playwright Bot
      #     SLACK_MESSAGE: 'E2E merge Tests failed!'
      #     SLACK_TITLE: 'Playwright E2E Tests - FAILED'
      #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
